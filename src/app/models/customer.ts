
export interface Customer {
        id: number | null; //nullable to support the add 
        firstName: string;
        lastName: string;
        email: string;
        created: number;
        updated: number;
}
